### Explanation

```markdown
  - always put transition in main class
  - :hover and :active are called pseudo-class
  - opacity values are between 0.0 ~ 1.0
  - one transition can have multiple properties values
  - box-shadow values guide: horizontal(-/+) vertical(-/+) blur color+opacity(rgba)
  ```